pywebview>=5.0
UnityPy>=1.10
Pillow>=10.0
requests>=2.31
protobuf>=4.25
sssekai>=0.7.12
coloredlogs>=15.0
# 以下为 azurlane.spec 打包时 collect_all 的依赖（sssekai 链路 + 语音），
# 缺失会导致 PyInstaller 打包阶段直接崩溃
pypinyin>=0.49
fmod_toolkit>=0.1.3
pyfmodex>=0.7.2
archspec>=0.2.6
humanfriendly>=10.0
